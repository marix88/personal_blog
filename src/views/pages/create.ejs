<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("../partials/head"); %> 
</head>
<body class="container">
  <header>
    <%- include("../partials/header"); %>
  </header>
      <div class="d-flex flex-column justify-content-center align-items-center h-30">
        <h1 class="text-red fw-bold">Add New Blog Post</h1>
      </div>
  <section id="cards" class="pt-5">
    <form 
    id="addBlogPostForm"
    method="POST"
    action="http://localhost:3000/create"
    class="forms"
  > <div>
      <select id="selectCategory" name="selectCategory" class="mb-3">
        <option selected>Choose category</option>
        <!-- put all categories in selectCategory -->
        <% for (let i=0 ; i<titleCategory.length; i++) { %>
          <option name="selectedCategory" value="<%= titleCategory[i] %>"><%= titleCategory[i] %></option>
        <% } %>
      </select>
      </div>
        <input
          type="text"
          id="title"
          name="title"
          class="input-text"
          placeholder="Your blog post title"
          required
        />
      <div class="mb-3">
        <input
          type="text"
          id="snippet"
          name="snippet"
          class="input-text"
          placeholder="Your blog post snippet"
        />
      </div>
      <div class="mb-3">
        <textarea
          name="content"
          placeholder="The content of your blog post"
          required
        ></textarea>
      </div>
      <div class="mb-3">
        <input type="date" name="createdAt" enabled/>
      </div>
      <div class="submit">
        <button type="submit" class="submit-btn btn-primary" name="submitPostBtn">
          SUBMIT
        </button>
      </div>
      <p id="createFormResponse"></p>
    </form>
  </section>
  <div id="space">
    <a href="/">See the latest blog posts</a>
  </div>
  <footer>
    <%- include("../partials/footer"); %>
  </footer>
  </body>
  <script type="module" src="/javascripts/newblogpost.js">
  </script>
</html>
